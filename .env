# Variables compartidas por docker-compose.
# El hostname por el que se sirve toda la app.
APP_HOST=crewfare.localhost

# Credenciales de MySQL. DEBEN coincidir con backend/.env.
DB_DATABASE=crewfare
DB_USERNAME=crewfare
DB_PASSWORD=secret
DB_ROOT_PASSWORD=root
