# Dependencies (rebuilt inside Docker)
backend/vendor/
frontend/node_modules/
frontend/dist/

# Local data
backend/database/database.sqlite

# Note: the .env files ARE committed on purpose (throwaway test environment,
# ready to share). In a real project you would NOT version them.
